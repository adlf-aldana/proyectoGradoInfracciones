import { ObteniendoDatosPersonal } from './obteniendo-datos-personal';

describe('ObteniendoDatosPersonal', () => {
  it('should create an instance', () => {
    expect(new ObteniendoDatosPersonal()).toBeTruthy();
  });
});
